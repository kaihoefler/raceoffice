import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    mode: "light",
    primary: { main: "#bcbcbcff" },
    secondary: { main: "#92b9f9ff" },
    background: { default: "#fffdfbff" },
  },
});
