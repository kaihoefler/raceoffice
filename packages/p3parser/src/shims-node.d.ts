/**
 * This file is intentionally kept as a placeholder.
 *
 * The package now relies on official Node.js type definitions via
 * `@types/node` and `types: ["node"]` in `tsconfig.json`, so no local
 * ambient shims are required anymore.
 */
export {};
